import useAxios from "../useAxios"
import { deviceConfig } from "./api"
import type { IDeviceGetListRequest, IDeviceResponse } from "./type"

export const useDevice = () => {
  // const { request: requestDeviceList } = useAxios<IDeviceGetListRequest, IDeviceResponse>(deviceConfig.getDeviceList)


  // requestDeviceList({
  //   projectId: string
  //   sort: string
  //   pageSize: number
  //   page: number
  // })
}